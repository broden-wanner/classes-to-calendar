/**
 * File to hold global settings variables used throughout the frontend
 */
const globals = {
    defaultStartDate: new Date(2021, 0, 19),
    defaultEndDate: new Date(2020, 4, 3)
};

Object.freeze(globals);

export default globals;
